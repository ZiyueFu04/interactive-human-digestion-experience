# Interactive Human Digestion Experience

A bilingual, single-page medical learning experience for the human digestive system.

Open `interactive-digestion.html` directly in a browser, or publish the repository with GitHub Pages and use `index.html` as the entry point.

## GitHub Pages

Use the repository root as the Pages source. The public page will redirect from `index.html` to the interactive HTML experience.
